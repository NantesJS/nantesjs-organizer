language: node_js
node_js: node
install:
  - npm ci
# keep the npm cache around to speed up installs
cache:
  directories:
  - "$HOME/.npm"
script:
  - npx jest --coverage && npx coveralls < ./coverage/lcov.info
