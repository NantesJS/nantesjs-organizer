language: node_js
node_js: node
install:
  - npm ci
# keep the npm cache around to speed up installs
cache:
  directories:
  - "$HOME/.npm"
script:
  - npx jest --setupFiles dotenv/config --coverage && npx coveralls < ./coverage/lcov.info
  - docker run --rm -i hadolint/hadolint < Dockerfile
